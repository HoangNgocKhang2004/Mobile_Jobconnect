enum AccountStatus { active, inactive, suspended }
enum Gender { male, female, other }
enum JobType { full_time, part_time, contract, internship }
enum ExperienceLevel { junior, mid, senior }
enum JobPostStatus { open, closed, draft }
enum JobApplicationStatus { pending, viewed, interview, accepted, rejected }
enum PreferredJobType { full_time, part_time, contract, internship, any }
enum PaymentStatus { pending, completed, failed }
enum PaymentMethod { card, bank_transfer, mobile_payment }
enum NotificationType { application_update, interview_reminder, job_post_update, payment_status, recommendation }
enum FeedbackType { job_post, company }
enum TestType { technical, aptitude, personality, other }
enum QuestionType { multiple_choice, open_ended, coding, other }
enum InterviewMode { online, in_person }
enum InterviewStatus { scheduled, completed, cancelled, rescheduled }
enum MessageType { text, file, image, url }